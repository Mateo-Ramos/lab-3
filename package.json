{
  "name": "Y",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "esbuild app/javascript/application.js --bundle --sourcemap --outdir=app/assets/builds",
    "build:css": "esbuild app/assets/stylesheets/application.bootstrap.scss --bundle --sourcemap --outfile=app/assets/builds/application.css --loader:.scss=css"
  },
  "devDependencies": {
    "esbuild": "^0.25.1",
    "esbuild-sass-plugin": "^3.3.1",
    "sass": "^1.86.0"
  },
  "dependencies": {
    "@rails/ujs": "^7.1.3-4",
    "bootstrap": "^5.3.3"
  }
}
